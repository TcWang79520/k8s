# 模擬考場既有的 /cks/sa/pod1.yaml：serviceAccountName 指定錯誤（backend 不存在、也不符合 -sa 結尾的命名規則）
apiVersion: v1
kind: Pod
metadata:
  name: pod1
  namespace: qa
spec:
  serviceAccountName: backend-sa
  containers:
  - name: nginx
    image: nginx:stable
